// src/types/user.ts

export interface User {
  _id: string;
  name: string;
  email: string;
  // Add more fields as needed (e.g. avatar, role, etc)
  // avatar?: string;
  // role?: 'admin' | 'seller' | 'buyer';
}
